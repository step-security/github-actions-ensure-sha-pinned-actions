{
  "name": "github-actions-ensure-sha-pinned-actions",
  "version": "5.0.7",
  "description": "Ensure that GitHub Actions are pinned to full length commit SHAs",
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint .",
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "jest --injectGlobals=false"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/step-security/github-actions-ensure-sha-pinned-actions.git"
  },
  "keywords": [
    "actions",
    "node"
  ],
  "author": "step-security",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/glob": "^0.7.0",
    "axios": "^1.19.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.44.1",
    "eslint": "^10.8.0",
    "jest": "^30.4.2"
  }
}